/** @type {import('next').NextConfig} */
const nextConfig = {
    /*  output: 'export',
     trailingSlash: true,
 
     basePath: '/origins',
     assetPrefix: '/origins',
 
     images: {
         unoptimized: true,
     }, */
}

module.exports = nextConfig
